import React from 'react';
import './DraggableIcon.css';

/**
 * Custom draggable icon to match the challenge prototype one. Ideally this should be a vector icon,
 * but I did not find any similar to the prototype and thought it would be best to only to recreate
 * it using CSS.
 */
const DraggableIcon = () => (
  <div className="DraggableIcon">
    <div />
    <div />
  </div>
);

export default DraggableIcon;
